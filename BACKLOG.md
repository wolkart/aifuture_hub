# Бэклог скиллов / харнеса

Захват улучшений скиллов **на ходу**: замечаешь косяк в момент использования (обычно из контентной сессии в `content-studio`) — кидаешь сюда одну строку и не тормозишь работу. Чинишь пачкой в отдельной hub-сессии.

**Только про скиллы/харнес.** Не для контент-идей, стратегии, клиентов, секретов — это приватное, ему здесь не место (репозиторий публичный).

Формат строки: `- [ ] <скилл>: <что не так / что улучшить> — <контекст, если важен>`
Сделано — отметь `- [x]` и/или унеси в раздел «Закрыто» с номером PR.

## Открыто

- [ ] **Тесты репо не гоняются одной командой: конфликт имён модулей.** `pytest skills/*/tests` падает на сборке — `test_render.py` и `test_check_env.py` есть и в `carousel-render/tests`, и в `rough-cut/tests`, а без `__init__.py` pytest считает их одним модулем. По отдельности каждый набор зелёный. Чинится любым из трёх: добавить пустые `__init__.py` в папки тестов, либо задать `rootdir`-конфиг с `--import-mode=importlib`, либо развести имена. Мешает единой команде «прогнать всё перед мерджем». Плюс к этому: полный прогон требует `--with numpy --with scipy` (зависимости `rough-cut` через PEP 723) — стоит записать рабочую команду в README хаба.
- [ ] **rough-cut: smart-cut как быстрый режим рендера** — рендер полностью перекодирует выход (libx264): замер 82с выхода 1080×1920@50fps = 173с. FCPXML-first для длинного входа уже вшит (PR#18), но для «нужен mp4 сразу из часового» стоит прикинуть stream-copy keep-кусков по ключевым кадрам (без перекода). Замеры 2026-07-11: whisper 12мин→110с (час≈10мин), тишина 12мин→10с, синхрон→2с.
- [ ] **База подходов (`тип: script`)** — конструкции ниже ждут схему. Когда возьмём `тип: script` в hook-base (или соседний склад): угол + скелет тела + текстура. Сид — карточки разведки + библиотека юзера (Ник/Косолапов/солянка). Брать, если после фикса границ (PR#14) проседает именно текстура тела.
- [ ] **reels-script: «бенд-шов» как обязательный шаг формата-обзора** — обзор сервиса легче всего скатывается в чистую халяву (тело-польза без разворота) → кормит туристов, переучивает алгоритм на халяву. Вшить в скилл: тело даёт пользу про сервис (не продаёт), а между телом и CTA — **один шов ≤2 предложений**, который поднимает «юзать сервис» → «строить/понимать», делая сервис примером, не целью. 5 бенд-ходов (коммодити→навык / инструмент→мышление / потыкать→собрать / зависимость→своё / догонять→ров) + правило «нет бенда → не публикуешь». Полная руда — в волте `2-Контент/Правило бенда — обзор в СТРОЙ.md` (приватное, сюда не тащим). Контекст: дрейф-чек воронки, автор миксует обзоры а-ля Ник Сараев.
- [ ] **Новый скилл `video-transcribe`** — вынести ASR в отдельный переиспользуемый скилл: на вход путь к локальному видео/аудио (или URL), внутри ffmpeg (аудио → моно 16 кГц mp3, чтобы влезть в лимит Whisper 25 МБ, при переборе — нарезка на чанки), затем OpenAI Whisper (`whisper-1`), на выход `.txt` + `.srt` (таймкоды) рядом с исходником. Ключ `OPENAI_API_KEY` уже есть. **Главный выигрыш — DRY:** `instagram-analytics` перестаёт держать свою копию ASR (`references/asr.md`) и делегирует сюда. Рабочий скрипт уже написан в контентной сессии — скилл будет обёрткой вокруг него. НЕ мини-сервис: веб-морда дублирует то, что и так даёт сессия (порт, процесс, поддержка — чистый оверхед).
- [ ] **`copy-law`: развести ленту и поиск — имя инструмента в тайтле YouTube РАБОТАЕТ.** Замер 2026-08-09 по двум каналам ниши независимо: имя инструмента/бренда в тайтле даёт **1.29× против 0.94×** (Сараев, n=87) и **1.26× против 0.75×** (Нейт Хёрк, n=182). Это прямо противоположно правилу ленты, где хук-метод проигрывает хуку-выгоде в 10–30 раз (замер 124 постов, вшит в `reels-script`). Противоречия нет: лента показывает то, что не просили — имя там пустой звук; YouTube поисковый — имя инструмента и есть запрос. Нужна оговорка в `copy-law §3`, иначе `youtube-meta` унаследует правило не с той площадки. Тот же замер: формат **курса** (9.96× / 1.97×) и формула **Build & Sell** (49× / 7×, но n=3 и n=2 — ниже порога надёжности) подтверждаются у обоих; а тег года, скобки и теги — приёмы лично Сараева: Нейт года не ставит ни разу, скобки у него в минус, теги лишь у 43 из 300 при 924k подписчиков.
- [ ] **Новый скилл `youtube-meta` (или расширение reels-script под длинные видео)** — **партнёр готов: `youtube-analytics` влит 2026-08-09**, он даёт разбор тайтлов и вступлений, но мету не пишет. Голос подключать посекционно, а не целиком: первые ~125 символов описания — только закон (`copy-law`, результат вместо термина), абзац «если ты впервые, я…» — голос целиком, ссылки/Summary/Chapters — ни то ни другое (правило уже записано в `brand-voice/SKILL.md`, таблица двух слоёв). `post-hq` НЕ трогать: описание YouTube не пост, пятая площадка размоет его контракт. — под длинное горизонтальное YouTube-видео нет генератора мета: тайтл / описание / хештеги / подсказка по превью по канонам. Всплывало дважды, оба раза поднимали веб-ресёрчем руками. Канон июль-2026 (проверено ресёрчем): Title ≤100 симв, хук+ключевик в первые ~50 (мобилка режет ~48–55), ключевик спереди, формат `Как + [ключевик] + выгода + (год)`, без эмодзи и клибейта; тег свежести `(год)` снимает «устарело?». Description: первые ~125 симв = мета (ключевик+хук+важная ссылка), 200–500 слов, плотность ключей 1–2%, без стаффинга; 3–5 хештегов точно по теме, первые 3 видны над тайтлом. **Эталон структуры описания — Ник Сараев** («How to Use Claude Code for FREE (2026)», 180K): оффер-ссылка(и) сверху → «если ты впервые, я…» (позиционирование) → ссылки на свои длинные видео (session time) → `Summary ⤵️` с ключевиками → инструменты → Chapters (таймкоды). Платформенные правила уже в памяти: YouTube CTA = ссылка (не «СТРОЙ»); мету не транскрибировать из видео; описание НЕ должно обещать того, чего в ролике нет (мисматч режет охват). Смежная запись под Shorts — ниже в списке идей. Честный SEO: чужие модели/способы упоминать можно, но рамкой «способ работает с X; в видео беру Y», не как показанное.
- [ ] **Точечные саб-агенты в контент-пайплайне** (вероятная доработка, НЕ срочно) — творческую цепочку (ai-research-идея → reels/carousel-script → brand-voice/post-hq) держим одним агентом последовательно (непрерывность контекста — правильный дефолт, не дробить). Два места, где саб-агент окупается по реальной причине, а НЕ ради красивого кадра в Pixel Agents: (а) **instagram-analytics / scout** — изоляция контекста: тяжёлый скрейп+транскрипты делает саб-агент у себя, в основную нить отдаёт только чистый ранжированный результат; (б) **reviewer-проход** для reels/carousel — отдельный агент-критик со свежими глазами проверяет стражей (тело не продаёт / хук из hook-base / 6 правил), честнее самопроверки. Оба — «добавляю, когда припекёт», не всегда. Контекст: всплыло при разборе расширения Pixel Agents (визуализатор Claude Code).

### Конструкции тела (сид для базы подходов — выжимка из библиотеки 3 авторов)

Сходятся у всех трёх авторов (Ник Сараев / Косолапов / солянка). Это скелеты ТЕЛА ролика под showcase/новость, НЕ хуки:

- **Анонс + халява + коммент-бейт:** «{кто} только что выкатил {что}» → 3–4 буллета возможностей → «и главное — бесплатно / open-source / без лимитов» → коммент-бейт «напиши кодовое слово — кину ссылку».
- **Отрицание лидера → замена:** «забудь про {лидер} / больше не плати за {лидер}» → чем новый инструмент лучше → как запустить (онлайн или локально) → CTA.
- **Иностранный авторитет → пошаговый туториал:** «этот индус/американец показал мне X» → 3–5 шагов с экрана → «готово за N секунд» → «сохрани / скинь другу».
- **Стыд через вопрос / «большинство делает неправильно»** → правильный способ → персональный результат у зрителя.
- **Сравнение / «убийца»:** «X стоит N, а тут 0» или «рвёт {лидеры} по бенчмаркам» → перечисление превосходств → «бесплатно / open-source» → CTA.
- **Сценка-эксперимент:** разыгранный мини-скетч/диалог → к середине вскрывается, что всё сделал ИИ → CTA.
- **История провала→успеха:** «я облажался N раз / разорился на подписках» → перелом → решение-сервис → CTA.
- **Нумерованный разбор ошибок:** «вот как правильно: 1, 2, 3» с контрастом «не делай так — делай так».
- **Пусть читатель посчитает сам:** описываешь не продукт, а ЧЕЛОВЕКА с маленьким ежедневным действием («100 комментов в день» / «30 минут вечером») → растягиваешь во времени → задаёшь вопрос «сколько у него будет через год?» → **пауза, отдельное сообщение** → только потом payoff. Читатель считает в голове, и своей цифре верит абсолютно. Две ветки: считать **выгоду** (действие × время = большое число) или **свою цену** («30 мин × месяц = 15 часов; прикинь, сколько ты листаешь ленту»). Ломается, если: числа не компаундятся (соврёшь), нет отдельного сообщения (читается как инородное тело), отвечаешь за читателя вместо паузы, подпираешь выдуманной статистикой. Требует места — строй сообщение ВОКРУГ приёма, не вставляй в готовое.

## Закрыто

- [x] **post-hq + reels-script: длинная discovery-форма подписи под рилс** — введена вторая форма IG-подписи под рилс (длинная пересказ-мини-статья под IG-поиск/Google-индексацию + caption dwell time), компаньон-дефолт для фото/каруселей цел. Правки: `instagram-caption.md` (раздел «Две формы подписи под рилс», канон структуры формы B, антиспам-планка, таблица длины, хэштеги 3–5 нишевых, чек-лист + антипаттерны); `reels-script/SKILL.md` (выдаёт подпись 2 формами, читая модуль как источник истины — без дублирования логики). Доказательная база: Мосери keywords>hashtags, индексация профаккаунтов Google с сер.2025, watch time всё ещё №1. Спека применена и удалена.
- [x] rough-cut (PR#18, боевой прогон рилса 2026-07-11): (1) **confidence синхрона** — нормированная корреляция давала ~0.1 на разнотембровых микрофонах при идеальной блокировке; заменил на peak-to-sidelobe → на боевом файле 0.107 → **0.938**, порог тревоги 0.2 → 0.5. (2) **probe rotation** — вертикаль (1920×1080 + displaymatrix 90) отдавалась горизонталью; читаю side_data/tags.rotate, свапаю стороны. (3) **режим «только тишина»** — новый `silence_cut.py` (автономный, без whisper/LLM: синхрон+тишина→cut-list) + явная маршрутизация в SKILL.md. (4) **FCPXML-first для длинного входа** + оговорка «CapCut таймлайны не импортирует, ему только mp4». 30→40 тестов.
- [x] hook-base: склад был перекошен в тип `обещание` (9/12) — наполнили библиотекой 3 авторов: **79 записей**, баланс по типам (обещание 19 / неожиданность 16 / провокация 14 / интрига 12 / отрицание 6) и нишам (ИИ + бизнес/финансы/маркетинг/саморазвитие/отношения). Дедуп по shortcode + близким шаблонам.
<!-- - [x] <скилл>: <что было> — PR#NN -->

- [reels-script/carousel-script] Уточнить дозировку CTA: «боль (Хормози) — топливо CTA» легко перегнуть на охватном контенте. Развести явно: охватный → CTA лёгкий на желании, мягкий посев; жёсткая боль только прогревочный. Сейчас правило есть, но провоцирует перегиб.

- [publishing/reels-script] Под YouTube Shorts генерить осмысленные заполнения по best practices: Title без хэштегов (ключевик+хук, ≤70 симв); Description обязателен (ключевое предложение с ключевиками + CTA + 2-3 КОНКРЕТНЫХ хэштега, НЕ #Shorts); backend-tags в Studio; подсказка по thumbnail (крупное лицо+эмоция+2-4 слова). Платформенный CTA: YouTube = ссылка в шапке профиля/описании (не кодовое слово «СТРОЙ» — это IG-коммент-бейт).

- [carousel-render] **Рендер 12 карточек = 75 с** (измерено). Причина не в счёте, а в холодных стартах Chrome: на карточку 1–3 запуска на замер переполнения (по одному на ступень кегля) + 1 на съёмку, каждый с `--virtual-time-budget=2000` → ~30 запусков по 2.2 с строго друг за другом. **Параллель проверена и отвергнута:** пул потоков даёт 11 карточек за 10 с, но 12-я вешается — Chrome держит singleton-lock на общем профиле; свой `--user-data-dir` на запуск (правильное решение) на Chrome 150 headless вешает процесс сразу, и с `--no-first-run` тоже. Значит остаются два настоящих пути: (1) **один запуск Chrome на всю карусель** — собрать длинную страницу со всеми карточками, снять одним кадром и нарезать `sips`; замеры так же одной страницей, читая data-атрибуты всех карточек сразу (~30 запусков → 2); (2) **кэш по хэшу** (слайд + тема + CSS лейаута): повторный прогон при том же входе уже даёт побайтово те же PNG, поэтому правка одного слайда не должна пересъёмывать остальные 11. Второй путь важнее: итераций на карусель всегда 2–4.
- [carousel-script] Добавить в «6 тестов перед выдачей» обязательный тест свайп-тяги: пройти КАЖДЫЙ слайд и ответить «зачем листать дальше?»; запретить слайды-каталоги в середине (выдают весь ответ разом и убивают петлю); главная петля закрывается только на предпоследнем слайде. Правило есть в carousel-backbone (блок 2), но в чеклист тестов не вынесено — из-за этого реально проскочила карусель с мёртвым слайдом 4/7.

- [carousel-script] Пересмотреть режим «платформа = обе»: сейчас SKILL.md предписывает спавнить субагента на каждый платформенный бриф и выдавать ДВЕ заточенные версии. На практике это удвоение ручной дизайн-работы (карточки рисуются в одном шаблоне) и оно бесит. Правильный дефолт: ОДИН комплект слайдов + сменный последний CTA-слайд (IG кодовое слово / LinkedIn Follow) + две подписи; лишний материал уводить в подпись, а не в платформенные слайды. Две версии — только по явному запросу. Заодно: поле «визуал-направление» сделать компактнее (что изображено), без описания палитры на каждой карточке — у автора единый визуальный шаблон.
- [instagram-analytics] Схема не ранжирует карусели: ER считается только при views>0, а IG не отдаёт просмотры каруселей. У каруселе-центричных аккаунтов (напр. @theromanknox: 90 из 100 постов — карусели) весь index уходит в хвост без сортировки. Ранжировать не-видео **по лайкам**, а НЕ по сумме likes+comments (решено 2026-07-30 на практике): коммент-бейт кодовым словом двигает только комменты (×4.5), лайки при этом не растут, поэтому сумма поднимает наверх посты с бейтом вместо тех, что зашли содержанием — для выбора референса это худший исход. Комменты оставить отдельной колонкой как диагностику бейта (отношение комм./лайки). Оговорка: лайки растут вместе с аккаунтом, поэтому длинную выборку сравнивать внутри месяца/квартала.
- [instagram-analytics + carousel-script] Ранжирование по метрике не отвечает на «годится ли это к переносу»: топ-2 @theromanknox по лайкам — витрины чужих курсов и плагинов, переносить нечего, и это выяснилось только после скачивания и просмотра карточек (дорого). В `carousel-script` проверка годности уже вписана (режим переноса, «витрина чужих продуктов»), но в разведке типа ценности нет. Добавить в `scout`/`patterns` дешёвый признак «тип ценности» (витрина чужих продуктов / система-инструкция / объяснялка / кейс) — определяется по подписи и первому слайду, без OCR всей карусели. Тогда `carousel-script` берёт следующий по ранжированию сразу, без дорогого прохода.
- [instagram-analytics] Добавить режим `patterns` (Tier-1.5, между scout и enrich): по уже собранной выборке считать не только ранжирование, а РАЗБОР паттернов — и это оказался самый ценный артефакт скилла. Считается на голых метриках, без ASR и почти без токенов. Обязательные срезы: (1) медиана вовлечения по месяцам — ловит спад/рост аккаунта и отвечает «его формат ещё работает или уже нет»; (2) коммент-бейт с кодовым словом vs без — медианы комментов, лайков и отношение комм./лайки (показывает, что бейт двигает ТОЛЬКО комменты, лайки не растут); (3) число слайдов в бакетах 1-5/6-8/9-11/12+ vs вовлечение; (4) длина подписи в бакетах <300/300-800/800-1500/1500+ vs вовлечение; (5) топ-25 и дно-15 с первыми двумя строками подписи — из них руками выводится типология хуков по МЕХАНИКЕ (разрыв статуса, невероятная цифра, «пока ты X — они тихо Y», момент провала), а не по теме; (6) для рилсов — просмотры vs тип хука (результат/ремесло → новость → имя инструмента). Плюс: тексты обложек тянуть **через `scripts/ocr-cards.sh`** (Apple Vision, ~0.3 с на карточку — сделано 2026-07-30). Ставка на поле `alt` датасета Apify **не сработала**: у @theromanknox там ровно «Photo by Roman Khaneichuk on January 25, 2026» без текста, авто-alt с «text that says…» приходит не всегда — как источник хуков не годится. Выход — отдельная заметка «Разбор — хуки и паттерны» в папке автора, с секцией «Что забираем себе». Образец: 2-Контент/Разведка/theromanknox/.
- [hook-base] Склад перекошен по ФОРМАТУ: все 79 записей — `формат: рилс`. Жёсткий фильтр по формату снят (2026-07-30, решение автора: рилсовый хук годится под обложку, если лаконичен и не завязан на видео — «сейчас покажу» и подобное не переносится). Осталось наполнить склад **карусельными** записями: у обложки другая длина и другой ритм, чем у первой фразы рилса. Сид — обложки разобранных каруселей из разведки (@theromanknox, @leadgenman) и свои, которые зашли.
- [carousel-render + carousel-script] **Формат «одна карточка»** — по разведке @leadgenman (200 постов) одиночные карточки собирают медиану 1994 лайка против 980 у каруселей и 380 у рилсов, при этом дешевле всех в производстве. Нужен лейаут, где текст занимает весь кадр (мысль/тейк/мем целиком, без свайпа), и режим в `carousel-script`, который выдаёт одну карточку, а не раскладку. Это же ответ на цель «3–5 единиц контента в день»: карусель на 17 карточек столько раз в день не сделаешь, одиночную — сделаешь.
- [carousel-script] **Рендер-лесенка: «ручной по умолчанию» читается автором как недоделка.** На запрос «сделай пост-карусель» скилл выдал текст + спек + JSON и написал «рендер не запускал» строкой в допущениях — автор спросил «а где же сами слайды?». Для него карусель = карточки, а не текст карусели. Правило дефолта переставить: платформа названа и текст готов → рендерить сразу (`carousel-render` уже стоит и настроен), ручной режим — только по явному «я сам». Как минимум — спрашивать одной строкой в конце, а не прятать в «Допущения».
- [carousel-script] **Код-слово перестать ставить по умолчанию.** Замер @leadgenman: без кодового слова медиана лайков выше в 1.7–6.5 раза во всех трёх форматах (карусели: 5924 против 902), комментарии при этом растут в десятки раз. Бейт покупает комментарии ценой охвата. Правило: раздачам и подборкам — да, тейкам и мемам — нет. Сейчас CTA-канон вешает «СТРОЙ» на всё.
- [carousel-render + carousel-script] **Лейаут `квиз`** — перенос формата-самопроверки @leadgenman (карточка DacA6mljPXb: 470 лайков / 222 коммента; механика «ответь себе ДА/НЕТ» гонит комменты сильнее кодового слова). Прогнали на боевой карусели 2026-08-01: квиз-слайд лёг на `тело` (вопрос + два блока ДА/НЕТ) и это рабочий выход. Остался один настоящий зазор: **в `тело` все блоки одного кегля**, поэтому вопрос не читается как заголовок — в референсе он вдвое крупнее ответов и держит карточку. Нужен лейаут с двумя зонами: вопрос крупно (ступень уровня `тело` 78) + ответы ступенью ниже, `ДА`/`НЕТ` бейджами. Шкала сложности (перчики) НЕ нужна — автор её отверг как чужой визуальный язык. Плюс в `carousel-script`: режим-квиз (обложка-провокация → N вопросов → CTA).
- [x] carousel-script: выбор лейаута шёл «по слову список», а не по объёму — квиз-слайд «вопрос + ДА/НЕТ» уходил в `тело-список` (кегль 42) вместо `тело` (78), карточки выходили мелкими с пустой нижней третью. В `slide-spec.md` добавлены ступени кегля прямо в таблицу лейаутов и раздел «Выбор лейаута — по ОБЪЁМУ текста»: 1–3 блока → `тело`, 4+ коротких пунктов → `тело-список`.
