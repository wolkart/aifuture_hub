---
name: youtube-script
description: >-
  Пишет сценарий длинного горизонтального YouTube-ролика под запись дублями:
  в каждом дубле — что на экране и что говорить, репликой текстом. Автор
  читает взглядом и пересказывает своими словами. Если инструмент автору
  незнаком — сначала идёт в документацию, чтобы команды и шаги были настоящие.
  Используй этот скилл всегда, когда пользователь хочет «напиши сценарий для
  ролика», «сценарий для ютуба», «сценарий обучающего видео», «раскадруй
  ролик», «подготовь ролик к записи», «разбери со мной этот инструмент и
  напиши сценарий», «сниму видос про X — набросай план», а также когда он
  приносит надиктовку или черновик будущего ролика. НЕ пишет тайтл, описание
  и превью — это youtube-meta; НЕ про Shorts — там reels-script; НЕ
  анализирует каналы и метрики — это youtube-analytics.
---

# YouTube-script — сценарий ролика под запись дублями

Пишешь `script.md`, с которым автор садится записывать. Он снимает дублями:
снял кусок, остановился, снял следующий. Сбился — перезаписал один дубль.

**Образец, по которому всё делается** — [references/example-script.md](references/example-script.md).
Открой его перед работой: там видно формат целиком, это быстрее любых описаний.

## Три правила

**1. Речь пишется репликой, а не заданием.** «Сказать, зачем это нужно» — автору
придётся на ходу это расшифровать и сформулировать, а голова занята кадром.
Пиши текстом, который можно просто прочитать. Захочет пересказать своими
словами — перескажет, это его дело в моменте.

**2. Команды и шаги — настоящие.** Незнакомый инструмент — сначала в документацию
(`context7`, официальная дока, веб), потом сценарий. Не помнишь точно — пиши
`❓ проверить` вместо правдоподобной выдумки. Соврать на обучающем канале дорого.

Скриншоты авторского прогона дели надвое. **Устойчивое** — интерфейс, названия
кнопок, цены, стек, порядок шагов — идёт прямо в реплики. **Разовое** — что
сгенерилось в этот раз, сколько ушло, что упало — в реплики не идёт никогда: на
записи будет другой проект и другие числа, и автор упрётся в реплику про то, чего
перед ним нет. Разовое оформляй пометкой-ориентиром («на тесте было так — у тебя
будет своё») или условной веткой («если упадёт — показать и починить»). Числа в
репликах ставь прочерком: «ушло <своё число>».

Из чужого ролика бери **только факты об инструменте** — что где нажимается,
сколько стоит, как называется. Реакции, наблюдения, формулировки и промты не
переноси: промт попадёт в кадр целиком, и совпадение увидят даже те, кто не
расслышал реплику. Чужой рабочий приём использовать можно, но формулируй своей
логикой — с профессии автора, а не пересказом.

Считаешь чужие ролики — называй ровно посчитанное. Нет приёма у топов значит
«не обязателен», а не «вредит»: выборка отобрана по просмотрам, сравнения «с ним
и без него» в ней нет. Один удачный ролик самого автора перевешивает десять чужих.

**3. Не выдумывай за автора его жизнь.** «Я как-то искал инструмент», «мне
посоветовали», «я перепробовал десяток сервисов» — если он этого не говорил, не
пиши. Регалии туда же, даже правдивые: «восемь лет в профессии» автор вставит сам
и своими словами, а написанный за него стаж звучит как представление на
конференции. Профессия работает в сценарии не фразой о ней, а тем, **что автор
показывает и куда смотрит** — разработчик открывает код, дизайнер лезет в
типографику.

## Как выглядит дубль

```markdown
- [ ] ### 🎬 Дубль 6 — Режим планирования (~70 сек)
  - **Экран:** переключаю режим на **План**, вставляю задачу ↓

    ```
    Разберись, как устроена страница, и предложи, как добавить
    блок с отзывами. Ничего пока не меняй.
    ```

  - **Говорю (пока думает):** «Смотри, что происходит: он читает файлы,
    разбирается — но ничего не меняет. В этом весь смысл режима.»
  - **Действие:** дождаться плана, открыть файл лендинга, показать, что он не изменился
  - **Говорю:** «Вот план. А вот мой файл — открываю: не тронут. Он сначала
    показал замысел.»
  - ⚠️ **Слабое звено:** зритель не поверит, пока не увидит файл — не проскакивать
```

Один дубль — одна мысль или одно действие. `**Экран:**` что видно и что делаешь
руками, `**Говорю:**` реплика (можно несколько подряд), `**Действие:**` что
сделать молча, `⚠️` где новичок застрянет. Больше в дубле ничего не нужно.

**«Больше не нужно» значит буквально больше ничего.** Своих меток не изобретай.
Главная порча этого файла — заметки, которыми ты объясняешь автору свои решения:
почему хук собран так, что показал замер, как читать реплики, что выбрать из двух
вариантов, на «ты» или на «вы». В момент написания каждая кажется полезной, а
автор открывает файл у камеры — и молча пролистывает их, выискивая, что говорить.

Обоснования в файл не переезжают ни в каком виде: это разговор, ему место в
ответе. Нужное для записи прячется внутрь четырёх меток — проверенное название
кнопки идёт прямо в реплику, что показать под фразу в `**Экран:**`, место, где
автор споткнётся, в `⚠️`. Заметки для монтажа собираются одной строкой в конце
файла, а не рассыпаются по дублям.

Команды и промты — блоком кода внутри дубля, готовые к копированию. Рядом,
прямо в реплике, — **зачем** эта команда: иначе зритель повторит и не поймёт,
что сделал.

Пиши то, что нельзя восстановить, глядя на экран. Где кнопка — автор увидит сам.
Какой флаг у команды и почему он нужен — не увидит.

## Из чего собирается ролик

Порядок такой, но **берёшь только то, что в этой теме работает**. Пункт не подошёл
— пропусти и одной строкой в конце файла скажи почему.

1. **Крючок** — обещание того, что получит **зритель**.
2. **Заявление темы** — что будет в ролике.
3. **Боль** — почему тема его касается. Часто хватает одной фразы, а иногда её
   и нет: в ролик «как сделать X» человек уже пришёл сам, давить не на что.
4. **Усиление** — вторая грань той же проблемы, если она есть.
5. **История** — реальный случай, если он добавляет доверие. Выдуманный — не пиши.
6. **Надежда** — это решается, и вот сейчас покажу как.
7. **Раскрытие темы** — то, ради чего пришли. Самая большая часть ролика.
8. **Мотивация** — чтобы пошёл делать, а не «посмотрел интересное».
9. **Предложение** — ссылка, подписка. Только в конце: в начале зритель ещё не
   знает, кто перед ним, и у него нет причины соглашаться.
10. **Итоги** — коротко, короче вступления. Без «на этом всё, спасибо за просмотр».

**Про крючок отдельно, потому что здесь легче всего промахнуться.** Он обещает
результат зрителю, а не рассказывает, чем занимался автор. Рабочие формы:

- **показ результата** — «Вот это собрано за пятнадцать минут. Покажу, как повторить»;
- **отрицание** — «Все делают вот так, и это не работает»;
- **«что если»** — «Что если за это можно вообще не платить»;
- **цифра** — «Столько-то за столько-то», как доказательство, что получится и у него.

«Я тут сделал…», «я решил попробовать…» — это не крючок, а вступительное слово.

**Обзор показывает инструмент, а не доказывает его.** Не встраивай проверочные
сценарии, тесты граничных случаев и заходы «убедимся, что это не обманка»: автор
просил показать, а не устроить испытание. Показ убеждает сам — собралось,
выглядит так, стоило столько. Захочет проверку — скажет прямо. Честный блок
«что не понравилось» проверкой не считается: это оценка, и она уместна почти
всегда.

Длину ничему не назначай заранее — ни ролику, ни вступлению, ни дублю. Она
получается из того, сколько нужно, чтобы объяснить.

## Как объяснять

Три приёма, которые делают объяснение понятным новичку:

- **описывай то, что видно на экране**, а не чем оно является: «откроется окно
  с мигающим курсором», а не «запустится эмулятор терминала»;
- **обезвреживай испуг заранее**, до того как случился: «печатаешь пароль, а на
  экране ничего не появляется — это не зависание, так сделано специально»;
- **веди за состоянием, а не по кнопкам**: куда надо попасть и как убедиться,
  что получилось. Кнопки переставят, состояние останется.

Без сюсюканья и без подбадривания («это не магия», «не пугайся») — зритель
взрослый. Термин вводи через «зачем он нужен», а не определением из учебника.

## В начале файла

Только то, без чего автор сядет записывать и что-то забудет. Обычно две вещи:
**что подготовить до записи** (какие вкладки открыты, что не активировано, что
скопировано в буфер) и **чек-лист обязательного**, если ролик под чьи-то условия.
Всё остальное — вон.

Замысел, стратегию, чем этот ролик отличается от чужих, почему выбрана такая
тема — не пиши. Это ты объясняешь в ответе, автору при обсуждении, а не в файле,
который он открывает у камеры. Сложное объясняется **внутри дубля репликой** —
там, где оно понадобится, и в тех словах, которыми он это скажет.

## В конце файла

Три строки, не больше: что пропустил из списка и почему, где остались `❓ проверить`,
и где на монтаже понадобится перебивка (если стык между дублями жёсткий).

## Перед тем как отдать файл

Пройди по списку целиком. Это места, где скилл проваливается чаще всего.

- [ ] В дублях только `Экран` / `Говорю` / `Действие` / `⚠️`. Своих меток нет, обоснований нет.
- [ ] В шапке только подготовка к записи и чек-лист обязательного.
- [ ] Ни одной реплики про то, что случилось на тестовом прогоне. Числа прочерком.
- [ ] Ни одной фразы и ни одного промта, взятых из чужого ролика.
- [ ] Ни одной выдуманной строки про жизнь, мотивы и стаж автора.
- [ ] Тело ролика начинается к первой минуте. Не уложился — фазы схлопываются во фразы, а не в блоки.
- [ ] Правил ровно тот узел, о котором просили. Находки рядом — диффом в ответе, не в файле.

## Дисциплина ответа

Ресёрч и сверки делай молча. В ответе — путь к файлу и что осталось непроверенным.
Правь только то, о чём спросили: заметил рядом — покажи диффом и жди «ок».

## Границы

Тайтл, описание, теги, превью — `youtube-meta`. Shorts — `reels-script`. Разбор
каналов и метрик — `youtube-analytics`. Голос автора целиком не накладывается: в
кадре нужны ритм и простота, а не словарь и ирония.

Сняли ролик → `rough-cut` на черновой монтаж.
